FROM node:18-alpine

WORKDIR /app

# package.json과 pnpm-lock.yaml 복사
COPY package.json pnpm-lock.yaml ./

# pnpm 설치
RUN npm install -g pnpm

# 의존성 설치
RUN pnpm install

# 소스 코드 복사
COPY . .

# Next.js 빌드
RUN pnpm run build

# 프로덕션 모드로 실행
EXPOSE 3000

CMD ["pnpm", "start"]